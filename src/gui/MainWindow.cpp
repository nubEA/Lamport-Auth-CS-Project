#include "MainWindow.hpp"

MainWindow::MainWindow(QWidget *parent)
    : QMainWindow(parent)
{
    // Your GUI setup code will go here.
    // For now, it just needs to not be empty.
    setWindowTitle("Lamport Auth");
}

MainWindow::~MainWindow()
{
}